<Window x:Class="DailyUptimeWidget.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification" Height="160" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" Focusable="False">
    <Window.Resources>
        <Storyboard x:Key="ShowAnim">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="100" To="0" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="HideAnim">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.5"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="40" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>
    
    <Border CornerRadius="24" BorderThickness="3" Margin="10">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#CC87CEFA" Offset="0"/>
                <GradientStop Color="#EE00BFFF" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.BorderBrush>
            <SolidColorBrush Color="White" Opacity="0.5"/>
        </Border.BorderBrush>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="#87CEFA" Opacity="0.4" ShadowDepth="0"/>
        </Border.Effect>
        <Border.RenderTransform>
            <TranslateTransform/>
        </Border.RenderTransform>

        <Grid Margin="30,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Bell Icon -->
            <Path Data="M12,22 C13.1,22 14,21.1 14,20 H10 C10,21.1 10.9,22 12,22 Z M18,16 V11 C18,7.9 16.4,5.4 13.5,4.7 V4 C13.5,3.2 12.8,2.5 12,2.5 C11.2,2.5 10.5,3.2 10.5,4 V4.7 C7.6,5.4 6,7.9 6,11 V16 L4,18 V19 H20 V18 L18,16 Z" 
                  Fill="White" Width="48" Height="48" VerticalAlignment="Center" Margin="0,0,24,0">
                <Path.Effect>
                    <DropShadowEffect BlurRadius="10" Color="White" Opacity="0.5" ShadowDepth="0"/>
                </Path.Effect>
            </Path>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TxtTitle" Text="Notification" Foreground="White" FontSize="26" FontWeight="Bold" FontFamily="Segoe UI Semibold"/>
                <TextBlock x:Name="TxtMessage" Text="Message content goes here." Foreground="White" FontSize="22" TextWrapping="Wrap" Opacity="0.9" FontFamily="Segoe UI" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
